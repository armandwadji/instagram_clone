import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  img: {
    height: 60,
    width: 60,
    borderRadius: 100,
    borderWidth: 2,
    borderColor: "white",
  },
});

export default styles;
